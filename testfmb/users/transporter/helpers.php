<?php
/**
 * Transporter pages use the application-wide helper set.
 * Keep this file as the module entry point so existing page includes do not
 * need to know the parent directory, while ensuring declarations are loaded
 * only once per request.
 */
require_once __DIR__ . '/../helpers.php';
